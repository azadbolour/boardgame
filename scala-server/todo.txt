
- Indicate that it is user's turn to drag.

- Remove non-word double letters from teh dictionary. WS, YE.

- Target container is not a DOM element error in browser console.

  Both main and index include the script. Only one should I think.

  I ran into this same error, and as others have suggested, it's because your
  bundle.js file is loading too early. Move your <script> tag into the body (as
  the last line before the closing </body> tag) to resolve this error. –

- Create new docker image with latest - uncommenting the deployment.

- Comment the deployment again.

- Indicate that first move has to be to the center.

- Help for the app.

- Create cleanup scripts for docker container and image.

- App logs in docker should go to the data area so you can see them easily from the
  outside.

- Word frequencies:

    http://norvig.com/ngrams/count_1w.txt
    Downloaded to /Users/azadbolour/Downloads/count_1w.txt.
    Unfortunately this is not a clean set. I'd say about 50% are non-words. So give up on that.

- https://www.wordfrequency.info/purchase.asp $250.00 for 100K words
  https://www.wordfrequency.info/files/entries.pdf 
    This is a clean list. But 100K is too few. The least frequent words are
    still relatively common.

  Can just use that for the machine play, and use Moby to check user plays.

  Credit Mark Davies.

- Truncate nohup.out on production machine.

- The port number specification is not dry. Make it so if possible.

- controller test is flaky because it has a timeout of 1 sec. Change to 3.

- On redhat trying to build the packager docker file:

  WARNING: The following packages cannot be authenticated!
  openjdk-8-jre-headless openjdk-8-jre openjdk-8-jdk-headless openjdk-8-jdk
  E: There are problems and -y was used without --force-yes
  Removing intermediate container 1f3aa35e0650
  The command '/bin/sh -c add-apt-repository ppa:openjdk-r/ppa   && apt-get update   && apt-get install -y openjdk-8-jdk && java -version'
  returned a non-zero code: 100

- Move current docker directory to haskell-server.

- Update root level README file.

- Upgrade Haskell server.

- Change docker names to reflect type of server -
  
    azadbolour/haskell-boardgame:0.2
    azadbolour/scala-boardgame:0.2
